{
  "name": "@graft/publish",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check-types": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@graft/check": "workspace:*",
    "@graft/core": "workspace:*",
    "@graft/db": "workspace:*",
    "@graft/runner": "workspace:*",
    "@graft/sandbox": "workspace:*",
    "@graft/toolbox": "workspace:*"
  },
  "devDependencies": {
    "@graft/config": "workspace:*",
    "@graft/sandbox-docker": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
