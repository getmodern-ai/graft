{
  "name": "@graft/email",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check-types": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "nodemailer": "^10.0.10",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@graft/config": "workspace:*",
    "@types/node": "catalog:",
    "@types/nodemailer": "^8.0.2",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
