# CLA signatures

Written by the CLA workflow, `.github/workflows/cla.yml` on `main`: `signatures/cla.json` records
who has signed `CLA.md`. The branch exists because `main` is protected and the bot cannot commit
there. Do not merge it, rebase it or edit it by hand.
