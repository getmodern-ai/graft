# GRA-46 report (the console wears Cando's shell, page frame and auth doors), PR #28

Saved by the orchestrator from the implementing agent's report, 2026-09-11.

## Report for the orchestrator — GRA-46

**PR #28** — https://github.com/getmodern-ai/graft/pull/28 — `feat(web): the console wears Cando's shell, page frame and auth doors (GRA-46)`, base `main`, head **`9d1fa836c54e92bef8f181c3c47772d558acde02`**, branch `aleks/gra-46-the-console-wears-candos-shell-sidebar-mobile-top-bar`. Five commits: shell + page frame + screens → auth doors + not-found/error/loader (+ `wifi_off` glyph) → AGENTS.md → hand merge of `origin/main` (GRA-49's #27, clean) → Greptile fix. Reviewer `sebapoole` requested; `OPEN`, `MERGEABLE`, `REVIEW_REQUIRED`. **Not merged.** GRA-46 is **In Review** in Linear with #28 attached. Worktree `gra-46` left in place, clean; the main graft checkout and Cando's tree untouched.

**CI / review.** `Typecheck, Lint & Test` pass on both heads (4m40s each), CLA pass. **Greptile 5/5 on `9d1fa83`** ("previously reported title synchronization issue is fixed and no new actionable failures"). Its one P2 (4/5 first pass): `useScreenTitle` registered in a passive `useEffect`, so the persistent bars could paint one frame with a stale/empty title. Valid — fixed with `useLayoutEffect` (cleanup and setup land in the same commit, before paint), verified live across all four rows, the breadcrumb and browser back with zero React warnings; replied inline, thread resolved.

**Gate (merged tree).** `check` no fixes (the two pre-existing `packages/mcp` warnings); `lint` clean; `check-types` 18/18 with the build; `test --force` 18/18, **`Cached: 0`** — web 83 (74 + `sidebar-state` 6 + `main-sidebar-nav-items` 3), server 114 incl. the Postgres integration suite on :5440; `check-colours` 111 sources clean; `check-tokens` 50/94 all resolve; CI's marker/ADR checks pass. `bg-scrim` confirmed on the Sheet overlay.

**Ported (Cando `origin/main`).** `skip-nav.tsx`, `lib/sidebar-state.ts` + test, `page/{page-header,page-container,page-nav,page-nav-collapsed-sidebar}.tsx`, `shell/sidebar-toggle.tsx`, `shell/mobile-top-bar.tsx`, `loader.tsx`, `route-not-found.tsx`, `route-error.tsx`, the `RailUser` menu contents (Theme radio group with the label inside, destructive Sign out), `AuthHeader` bands and the `SignInCard` frame, `sign-out.ts`'s shape, close-on-`onBeforeNavigate`. **Left out** (no such thing in Graft, ADR 0017): agent rail + `mobileLeading`, agent identity in the header, New Thread, Automations/Recents groups, settings shell, `--sidebar-width` override and `left-12` offset, face strip, Google sign-in, `settingsTarget`.

**Deviations, and why** (all in the PR body's delta list): the strip is mounted once in the shell fed by one title slot (`shell/screen-title.tsx`, `useScreenTitle`) rather than per screen — Graft's screens differ only in title, and no screen can forget the collapsed toggle; account trigger is a `SidebarMenuButton size="lg"` row (no rail, no Avatar primitive — initial in a disc with `AvatarFallback`'s tokens), no Settings item in the menu; pending count is `SidebarMenuBadge` (`aria-hidden`) plus an `sr-only` count inside the link → announces "Pending actions, 1 ask waiting" (verified with a throwaway ask over MCP, then declined; three "GRA-46 smoke" agents created and **revoked** in the local Docker DB); `PageNavBreadcrumb` (Cando's automation-detail strip as a component) titles the two detail screens and retires their ghost back buttons; `Kbd` primitive for the ⌘B hint; `GraftMark` inline SVG in tokens (no `dark:invert`); doors drop the card descriptions (Cando's frame is heading-only), no `md:pb-40`; `RouteError` offline check is `instanceof TypeError`; "Go to agents" not "Go home". One real defect found by the 390 capture and fixed: the agent detail's grid needed `grid-cols-1` (implicit `auto` track let the `<pre>` overflow the viewport).

**Behaviour verified** live (:3001 → Docker :3100, 40/40 scripted checks): form sign-in; first Tab → skip link, Enter → `#main-content`; ⌘B collapses, writes `sidebar_state=false`, survives reload, strip toggle expands; Theme → Dark persists and rewrites both `theme-color` metas to `#0f0c0a`; drawer opens from the hamburger and closes on navigate at 390; no horizontal overflow on any 390 screen; Sign out → `/login`, session `null`, guard redirects. One 401 per run: the sidebar's pending poll firing once as the shell unmounts after sign-out (same ordering as the old shell).

**Screenshots** (33) under graft-cloud `docs/screens/gra-46/`, captured on the merged tree with GT Standard shipping: `{login,agents,agent-detail-hermes-laptop,pending,connections,settings}-{1440x1024,390x844}-{light,dark}.png`, `agents-collapsed-1440x1024-{light,dark}`, `drawer-open-390x844-{light,dark}`, `account-menu-open-1440x1024-{light,dark}`, `agents-pending-badge-1440x1024-light`, `pending-with-ask-1440x1024-light`, `signup-1440x1024-dark`.

**Left for GRA-47:** tables onto `DataTable`, the central chip map, and the screen-level `Empty`s still drawing `rounded-lg border` (route-level ones now match Cando); `ConnectionPicker`/notice frames unchanged. Noticed: `PageContainer` gap is a per-screen `gap-6` (Cando passes `gap-4` on connections) — a GRA-47 call. **For you:** merge after sebapoole's approval; GRA-46's acceptance checkboxes in Linear are untouched.
## Orchestrator follow-up (2026-09-11)

The builder's `GraftMark` was an invented letterform (read as a P). Sent back before merge: it is now the placeholder letter G (`font-medium`, SVG text centred in the same `rx=7` `fill-foreground` square, letter in `fill-background`, no `dark:invert`), with the component comment saying it is a placeholder until Graft has a brand. Head `a61aad2`; CI green; Greptile 5/5. The 33 screenshots predate this fix and show the earlier glyph at the two small mounts only.
