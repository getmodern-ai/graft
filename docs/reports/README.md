# Build reports: the index

**A ticket's build record is its pull request body.** It says what landed and where, the contracts
later tickets were told to match, what was verified by hand and what was deliberately not, the
deviations from the ticket and their reasons. Read it before building on the ticket. Linear holds
the ticket itself and its outcome — the acceptance criteria, the comments, the findings a spike left
when it had no pull request — and `docs/adr/` holds the decisions. A report is none of those: it is
how one ticket was built, on the day it was built, and the pull request is where that already lives.

This file is the index over those bodies, one row per ticket, so a ticket number found in a
comment or an ADR leads to its record in one step. It is a snapshot with a date on it, like the
bodies it points at; a pull request merged after it was last edited is not in it.

The orchestrator kept its own copy of each implementing agent's final report while the tickets
were being built. Measured against the bodies, those copies were mostly redundant — for every
recent ticket the pull request body was larger than the report — so they were not committed. Where
a report held something its pull request body lacked (exact type declarations, wire shapes, a
decision recorded after the body was written), that content was appended to the body on
2026-09-17 under a heading `## Build report (added 2026-09-17 from the orchestrator's record)`,
verbatim apart from local paths. Fourteen bodies carry one: graft #4, #6, #7, #8, #9, #12, #14, #16,
#17, #18, #23 and #34, and graft-cloud #1 and #5.

## Reference documents kept here

Four documents no single pull request owns, kept because the work that read them cites them:

- `design-alignment-cando-design-system-survey.md` and `design-alignment-graft-console-survey.md`
  — the two read-only surveys ADR 0017's console alignment (GRA-44 through GRA-49) was planned from,
  2026-09-11.
- `cando-inventory.md` and `modern-inventory.md` — the two copy inventories ADR 0011's port from
  Cando and Modern was planned from, 2026-09-09.

All four had their local filesystem paths replaced with repo-relative paths or a phrase before
being committed; nothing else was changed.

## Where the rest is

- The hosted form's pull requests — the private backings, the Pulumi projects, the first deploy —
  are in getmodern-ai/graft-cloud, whose `docs/reports/README.md` indexes them and whose
  `docs/screens/gra-<N>/` holds the console screenshots the design-alignment bodies cite.
- GRA-25, the OpenClaw spike, produced no pull request; its findings are a comment on the ticket.
- Pull requests [#1](https://github.com/getmodern-ai/graft/pull/1) and
  [#2](https://github.com/getmodern-ai/graft/pull/2) (2026-09-08) predate the tickets: the required
  check and Dependabot mirrored from Cando, and the decision-record check's external-citation rule.

## Index

A bare number is a pull request in this repository. Where a ticket has two, the second follows the
first in the outcome.

| Ticket | PRs | Merged | Outcome |
|---|---|---|---|
| [GRA-2](https://linear.app/get-modern/issue/GRA-2) | [#3](https://github.com/getmodern-ai/graft/pull/3) | 2026-09-09 | scaffold the workspace and give CI its typecheck, lint and test steps |
| [GRA-3](https://linear.app/get-modern/issue/GRA-3) | [#4](https://github.com/getmodern-ai/graft/pull/4) | 2026-09-09 | the check learns SDK binding and derives the annotations; the runner gains proxyBase |
| [GRA-4](https://linear.app/get-modern/issue/GRA-4) | [#5](https://github.com/getmodern-ai/graft/pull/5) | 2026-09-09 | the sandbox seam, an in-process fake, the Docker backing and a conformance suite in CI |
| [GRA-5](https://linear.app/get-modern/issue/GRA-5) | [#6](https://github.com/getmodern-ai/graft/pull/6) | 2026-09-09 | the proxy reaches a vendor through a host segment, with the placeholder credential stripped |
| [GRA-6](https://linear.app/get-modern/issue/GRA-6) | [#7](https://github.com/getmodern-ai/graft/pull/7) | 2026-09-09 | persons, agents, connections and the toolbox rows, with services that run without a database |
| [GRA-18](https://linear.app/get-modern/issue/GRA-18) | [#8](https://github.com/getmodern-ai/graft/pull/8) | 2026-09-09 | the toolbox storage seam, the package policy and a publish that vendors under it |
| [GRA-19](https://linear.app/get-modern/issue/GRA-19) | [#9](https://github.com/getmodern-ai/graft/pull/9) | 2026-09-09 | an agent connects over MCP and sees its working set |
| [GRA-20](https://linear.app/get-modern/issue/GRA-20) | [#11](https://github.com/getmodern-ai/graft/pull/11) | 2026-09-09 | the backings selector; the private package is absent by default (the backings themselves: graft-cloud #1) |
| [GRA-23](https://linear.app/get-modern/issue/GRA-23) | [#12](https://github.com/getmodern-ai/graft/pull/12) | 2026-09-09 | reads pass, writes ask once, destructive asks every time |
| [GRA-24](https://linear.app/get-modern/issue/GRA-24) | [#10](https://github.com/getmodern-ai/graft/pull/10) | 2026-09-09 | the working set contracts by cap and idle window |
| [GRA-25](https://linear.app/get-modern/issue/GRA-25) | — | — | the OpenClaw spike: go, as the second harness; findings as a comment on the ticket |
| [GRA-26](https://linear.app/get-modern/issue/GRA-26) | [#13](https://github.com/getmodern-ai/graft/pull/13) | 2026-09-09 | the console: sign in, agents, pending actions, working sets |
| [GRA-28](https://linear.app/get-modern/issue/GRA-28) | [#14](https://github.com/getmodern-ai/graft/pull/14) | 2026-09-09 | the agent proposes a connection and the person enters the secret |
| [GRA-29](https://linear.app/get-modern/issue/GRA-29) | [#16](https://github.com/getmodern-ai/graft/pull/16) | 2026-09-09 | acquire builds a tool with a scripted model |
| [GRA-30](https://linear.app/get-modern/issue/GRA-30) | [#18](https://github.com/getmodern-ai/graft/pull/18) | 2026-09-09 | authorization-code OAuth with a client the person registered, guided |
| [GRA-31](https://linear.app/get-modern/issue/GRA-31) | [#19](https://github.com/getmodern-ai/graft/pull/19) | 2026-09-09 | a real model authors, the evals score it, the Hermes skill points at it |
| [GRA-33](https://linear.app/get-modern/issue/GRA-33) | [#17](https://github.com/getmodern-ai/graft/pull/17) | 2026-09-09 | one image and a compose file run the whole loop |
| [GRA-37](https://linear.app/get-modern/issue/GRA-37) | [#15](https://github.com/getmodern-ai/graft/pull/15) | 2026-09-09 | run the workflow on every pull request, not only those against main |
| [GRA-38](https://linear.app/get-modern/issue/GRA-38) | [#20](https://github.com/getmodern-ai/graft/pull/20) | 2026-09-09 | the image carries a linked backings package the bundled server can load (the package's build: graft-cloud #3) |
| [GRA-39](https://linear.app/get-modern/issue/GRA-39) | [#21](https://github.com/getmodern-ai/graft/pull/21) | 2026-09-09 | a backing may own the toolbox store; a store conformance suite (the drive-backed store: graft-cloud #5) |
| [GRA-41](https://linear.app/get-modern/issue/GRA-41) | [#22](https://github.com/getmodern-ai/graft/pull/22), [#24](https://github.com/getmodern-ai/graft/pull/24) | 2026-09-09, 2026-09-11 | override drizzle-kit's transitive esbuild to the 0.25 line; then the override lives in pnpm-workspace.yaml, where pnpm 10 reads it |
| [GRA-42](https://linear.app/get-modern/issue/GRA-42) | [#23](https://github.com/getmodern-ai/graft/pull/23) | 2026-09-11 | an elicitation accept without allow is an allow, for Hermes' approval buttons |
| [GRA-43](https://linear.app/get-modern/issue/GRA-43) | [#67](https://github.com/getmodern-ai/graft/pull/67) | 2026-09-18 | an elicitation declined faster than a person could read it is a dismissal, and falls through to the console |
| [GRA-44](https://linear.app/get-modern/issue/GRA-44) | [#25](https://github.com/getmodern-ai/graft/pull/25) | 2026-09-11 | the console takes Cando's tokens, icons and theme, with the token guards |
| [GRA-45](https://linear.app/get-modern/issue/GRA-45) | [#26](https://github.com/getmodern-ai/graft/pull/26) | 2026-09-11 | Cando's primitives replace the console's; selects, confirmations and code blocks become primitives |
| [GRA-46](https://linear.app/get-modern/issue/GRA-46) | [#28](https://github.com/getmodern-ai/graft/pull/28) | 2026-09-11 | the console wears Cando's shell, page frame and auth doors |
| [GRA-47](https://linear.app/get-modern/issue/GRA-47) | [#30](https://github.com/getmodern-ai/graft/pull/30) | 2026-09-11 | screens follow Cando's patterns: data tables, settings rows, empties, toasts and status chips |
| [GRA-48](https://linear.app/get-modern/issue/GRA-48) | [#29](https://github.com/getmodern-ai/graft/pull/29) | 2026-09-11 | the OAuth callback page is drawn by the console |
| [GRA-49](https://linear.app/get-modern/issue/GRA-49) | [#27](https://github.com/getmodern-ai/graft/pull/27) | 2026-09-11 | GT Standard L and GT Standard Mono ship with the console |
| [GRA-52](https://linear.app/get-modern/issue/GRA-52) | [#31](https://github.com/getmodern-ai/graft/pull/31) | 2026-09-15 | destructive tools ask once; asking every time is an opt-in per tool |
| [GRA-53](https://linear.app/get-modern/issue/GRA-53) | [#33](https://github.com/getmodern-ai/graft/pull/33) | 2026-09-15 | MCP clients connect over OAuth and the consent mints the agent |
| [GRA-54](https://linear.app/get-modern/issue/GRA-54) | [#32](https://github.com/getmodern-ai/graft/pull/32) | 2026-09-15 | the handshake carries the playbook; instructions and descriptions stand in for the skill |
| [GRA-55](https://linear.app/get-modern/issue/GRA-55) | [#34](https://github.com/getmodern-ai/graft/pull/34) | 2026-09-15 | an elicitation the client cancels falls back to the handoff link |
| [GRA-62](https://linear.app/get-modern/issue/GRA-62) | [#84](https://github.com/getmodern-ai/graft/pull/84) | — | assertCloudBackings holds a hosted provider's connect shape to the type: a relay scheme on none and link, the proxy's signing schemes on a form |
| [GRA-81](https://linear.app/get-modern/issue/GRA-81) | [#58](https://github.com/getmodern-ai/graft/pull/58) | 2026-09-17 | sign in with Google or GitHub, through Cando's one door; ADR 0020 |
| [GRA-82](https://linear.app/get-modern/issue/GRA-82) | [#60](https://github.com/getmodern-ai/graft/pull/60) | 2026-09-18 | forgot password: Cando's email transport and the reset flow, Loops when a key is set; ADR 0021 |
| [GRA-90](https://linear.app/get-modern/issue/GRA-90) | [#65](https://github.com/getmodern-ai/graft/pull/65) | 2026-09-18 | mail is a seam: the vendor transport and its template ids leave the open core; ADR 0021 rewritten |
| [GRA-89](https://linear.app/get-modern/issue/GRA-89) | [#66](https://github.com/getmodern-ai/graft/pull/66) | 2026-09-18 | sign-in endpoints are not hosts: set aside before provider coverage, dropped from the row, named in the answer; ADR 0019 amended |
| [GRA-92](https://linear.app/get-modern/issue/GRA-92) | [#69](https://github.com/getmodern-ai/graft/pull/69) | 2026-09-18 | an SMTP transport: the open form's working mail backing, proven against Mailpit |
| [GRA-94](https://linear.app/get-modern/issue/GRA-94) | [#73](https://github.com/getmodern-ai/graft/pull/73) | 2026-09-18 | registering opens no session until the address is verified; two doors; the verification and account-exists emails |
| [GRA-100](https://linear.app/get-modern/issue/GRA-100) | [#80](https://github.com/getmodern-ai/graft/pull/80) | 2026-09-19 | three observability seams with no backing in the open form — the log drain, analytics, model telemetry — and the MCP tool call on the wide event; ADR 0002 amended: no vendor in the open repository (the hosted half: graft-cloud #30) |
| [GRA-84](https://linear.app/get-modern/issue/GRA-84) | [#71](https://github.com/getmodern-ai/graft/pull/71) | — | the build approval and the keyless connection confirmation as in-chat cards through MCP Apps, answered by an app-only tool; ADR 0006 amended |
| [GRA-86](https://linear.app/get-modern/issue/GRA-86) | [#81](https://github.com/getmodern-ai/graft/pull/81) | — | Dependabot's minor-and-patch group lands: Better Auth 1.7.3 withdrew the `account.issuer` it required in 1.7.0 through 1.7.2, so the schema is regenerated with the renamed `auth` CLI and migration 0008 drops the column and its unique pair |
| [GRA-63](https://linear.app/get-modern/issue/GRA-63) | [#82](https://github.com/getmodern-ai/graft/pull/82) | — | the dry-run-before-any-ask eval scorer orders asks by the job's settle point in the world's record, not by a millisecond clock |
| [GRA-68](https://linear.app/get-modern/issue/GRA-68) | [#85](https://github.com/getmodern-ai/graft/pull/85) | — | a revoked connection is refused connection_revoked, and a relay row whose link never finished names the provider that holds nothing yet |
| [GRA-69](https://linear.app/get-modern/issue/GRA-69) | [#83](https://github.com/getmodern-ai/graft/pull/83) | — | a revoked connection leaves the agent's list: no execute tool, its promoted tools demoted with cause revoke, list_changed to live sessions, a call against it refused as connection_revoked; ADR 0009 amended |
| [GRA-104](https://linear.app/get-modern/issue/GRA-104) | [#87](https://github.com/getmodern-ai/graft/pull/87) | — | a connection the person holds but this agent was not given is the `scope` ask — `awaiting_scope`, a console card and the ask card with Allow, Decline and the build choice — not a `connection_exists` refusal; ADR 0006's card amendment names the third card |
| [GRA-105](https://linear.app/get-modern/issue/GRA-105) | [#88](https://github.com/getmodern-ai/graft/pull/88) | — | a person's connections reach every agent of theirs by default — `agent.scope_mode` `all` or `listed`, migration 0009 keeping every existing agent on its list, the scope resolved in one statement before any token is minted, the grant-on-connect a no-op under `all`, the mode as a `Select` in the create dialog, the consent card and the agent page; ADR 0007 amended |
| [GRA-112](https://linear.app/get-modern/issue/GRA-112) | [#90](https://github.com/getmodern-ai/graft/pull/90) | — | an awaiting result is not an MCP error: every `awaiting_*` result returns `isError: false` with the same JSON, since a host renders no view for an error result (ext-apps issue 694); ChatGPT's `openai/outputTemplate` and `openai/widget*` aliases beside the ask card's `_meta.ui` keys; ADR 0006's card amendment gains the sentence |
| [GRA-115](https://linear.app/get-modern/issue/GRA-115) | [#91](https://github.com/getmodern-ai/graft/pull/91) | — | `find_tool` matches every word of the query, in any order, across vendor, name (hyphens as spaces) and description, ranked name over vendor over description then by name hits; the rule is the pure `tools/find-tool.match.ts` with its own suite, and the description gains one clause |
| [GRA-114](https://linear.app/get-modern/issue/GRA-114) | [#92](https://github.com/getmodern-ai/graft/pull/92) | — | every meta-tool declares both hints outright: `destructiveHint: false` on every fixed tool but `run_tool` and `execute__<id>` (the hint is the carried request's) and `write_file` and `run_command` (both reach the shared toolbox mount), each with one sentence saying why, since MCP reads an unset hint as true and ChatGPT's Actions list tagged six tools DESTRUCTIVE for it; `session.test.ts` pins both hints per tool |
| [GRA-111](https://linear.app/get-modern/issue/GRA-111) | [#93](https://github.com/getmodern-ai/graft/pull/93) | — | descriptions describe, instructions instruct: every meta-tool, authoring and execute description is a third-person capability statement (what it does, its arguments, its answer shapes, the `awaiting_*` shapes and their `url`) opening with when it is used as a fact, since ChatGPT's classifier badged GRA-54's rule-bearing descriptions "Suspicious Instruction" on every call; every rule of conduct lives in `SERVER_INSTRUCTIONS` and the skill; `INSTRUCTIONS_BUDGET` is 2,048, Claude Code's documented per-server cap on both fields, and every description is held to it; `session.test.ts` scans every definition against a denylist of conduct markers and second-person words |
| [GRA-116](https://linear.app/get-modern/issue/GRA-116), [GRA-117](https://linear.app/get-modern/issue/GRA-117), [GRA-118](https://linear.app/get-modern/issue/GRA-118) | [#94](https://github.com/getmodern-ai/graft/pull/94) | — | every ask settles in the card: the `tool` ask is the fourth card, answered `{ allow }` through `recordApprovalAnswer`; a link provider's connect starts from the card through the app-only `start_link`, minting through `mintProviderLink` — the function the console's button now calls too; a secret's page opens as a popup with `from=card` and closes itself; the card polls the app-only `ask_status` until answered; the three app-only tools share `tools/card-gate.ts`; ADR 0006's card amendment names the fourth card and narrows what keeps the session to the secret |
| [GRA-116](https://linear.app/get-modern/issue/GRA-116) | [#95](https://github.com/getmodern-ai/graft/pull/95) | — | the tool ask's card renders: `run_tool`, every `execute__<id>` and every authored tool in the list carry `_meta.ui.resourceUri` and ChatGPT's alias, since a host renders a card only for a tool whose definition names the resource and the tool ask's `awaiting_approval` comes back through those tools, not the three GRA-84 marked; the card draws nothing for a result that is not an ask; found on #94's live check, 2026-09-20 |
| [GRA-120](https://linear.app/get-modern/issue/GRA-120) | [#96](https://github.com/getmodern-ai/graft/pull/96) | — | under a rendered card the awaiting answer says so: for a session whose client passes the card gate's client half (`card-client.ts`'s `clientRendersCards`, held once per session) the `message` takes its card form — the ask is on a card in this conversation, the url is the console for a person who cannot see it — and `cardShown: true` rides beside `url`; `SERVER_INSTRUCTIONS` gains the one clause (2,032 of 2,048) and the Hermes skill does not; a static-token agent's answer is byte for byte what it was; ADR 0006's card amendment gains the dated paragraph |
| [GRA-121](https://linear.app/get-modern/issue/GRA-121) | [#97](https://github.com/getmodern-ai/graft/pull/97) | — | `request_connection`'s description is the capability core alone, about a third shorter, and its `scheme` property carries no table: the parameters a proposal carries per scheme sit on `schemeConfig`, and nothing the person types (the secret fields, the OAuth client id) is named anywhere in the definition, since ChatGPT's classifier badged the definition "prescribes how to classify credentials, risk, and approval outcomes" after GRA-111; `session.test.ts` pins that the description says "on by default" and never "approval"; ADR 0006's card amendment notes the post-deploy card miss on Claude.ai as the host's (GRA-124) |
| [GRA-125](https://linear.app/get-modern/issue/GRA-125) | [#99](https://github.com/getmodern-ai/graft/pull/99) | — | patience is the protocol's: `acquire` holds its call for the approvals' wait and answers the settled job with `result` when it finishes in time, and `acquire_status { jobId, after? }` holds until a progress line newer than the caller's or the end, up to twenty seconds, so a chat model that polled seven times in twelve seconds and then ran its own code through `execute__` gets news on every call instead; the instructions and the Hermes skill drop "every ten to twenty seconds" |
| [GRA-122](https://linear.app/get-modern/issue/GRA-122) | [#98](https://github.com/getmodern-ai/graft/pull/98) | — | a tool follows its reconnected connection: a link provider's return reconnects a released row of the same provider and vendor whatever primary host and hosts the proposal names within the coverage — id, primary host and name kept, hosts widened to the union, the most recently revoked preferred; a run against a revoked default follows the one live connection of the vendor in the agent's scope and rebinds the tool, several are named under `alternatives`; `AuthoredRunArgs.connectionId`, which `acquire`'s dry run passes as the job's, and a publish under `activate: false` rebinds an existing row to it; the job's "did not run" line and `tried[].summary` carry a refusal's reason and message; ADR 0007 amended 2026-09-20; the brief's move of `primaryHost` deliberately not done, since the proxy prepends its path to every module path |
| [GRA-103](https://linear.app/get-modern/issue/GRA-103), [GRA-126](https://linear.app/get-modern/issue/GRA-126) | [#101](https://github.com/getmodern-ai/graft/pull/101) | — | the Pipedream provider leaves the open repository whole — client, relay plugin, provider, `GRAFT_PIPEDREAM_*`, fakes and proof scripts — for graft-cloud's private package, which decides coverage by asking Pipedream's catalogue (`allowed_domains`, `proxy_enabled`) with a cache rather than a hand-written vendor table, so Google Calendar and every proxy-enabled app are covered; `covers` and a link's `target` go async and `providerFor` awaits in order; the relay scheme is the generic `relay` (migration 0010 moves the rows), the proxy's `RELAYS` catalogue goes and the plugin rides the resolution; the open suites drive `@graft/core`'s fake link provider; ADR 0019 amended, ADR 0002's exception closed |
| [GRA-125](https://linear.app/get-modern/issue/GRA-125) | [#100](https://github.com/getmodern-ai/graft/pull/100) | — | a chat product's agent holds no pen: an agent held over OAuth lists the meta-tools and its promoted tools alone — no authoring set, no `execute__` — and a call to one is refused `advanced_tools_hidden`; `find_tool` answers `connections` with the `connectionId` `acquire` takes; measured on a fresh account where Claude called `execute__` for a weather question and ChatGPT ran its own code mid-job; ADR 0004 amended |
| [GRA-129](https://linear.app/get-modern/issue/GRA-129), [GRA-128](https://linear.app/get-modern/issue/GRA-128), [GRA-123](https://linear.app/get-modern/issue/GRA-123) | [#102](https://github.com/getmodern-ai/graft/pull/102) | — | a session the process no longer holds is re-opened for a chat product's client: a `grfta_` request with an unknown session id, or none and no `initialize`, gets a session under that id primed by a synthetic `initialize` (ADR 0018 as amended 2026-09-20), so Claude's card frame no longer draws "Unable to reach Graft" after a deploy, while a static-token agent keeps the spec's 404 and 400; the link card's Scheme row says how the provider connects rather than the keyring label the proposal carries; a publish refused only as `draft-missing` asks the toolbox store once more before the model is shown it. |
| [GRA-129](https://linear.app/get-modern/issue/GRA-129) | [#103](https://github.com/getmodern-ai/graft/pull/103) | — | a re-opened session stays with the agent that got it: `handleAs` is the one place a request reaches a transport, on the established path and after a re-open alike, so two agents presenting one unknown id at once get one 200 and one `session_mismatch` — Greptile's post-merge finding on #102. |
| [GRA-131](https://linear.app/get-modern/issue/GRA-131) | [#104](https://github.com/getmodern-ai/graft/pull/104) | — | a refused `/mcp` request names its reason on the wide event: `McpDeps.onTransportRefusal` carries the status, the JSON-RPC code and the answer's own sentence for Graft's 401/404/400 and for any 4xx the SDK's transport answers (read from a clone; the client's body untouched), and the server logs it under `mcpRefusal` — a bare 400 in the log was a guess in GRA-124 and GRA-129. |
| [GRA-130](https://linear.app/get-modern/issue/GRA-130) | [#110](https://github.com/getmodern-ai/graft/pull/110) | — | `input_invalid` on `request_connection` and `acquire` is self-correcting: it names only the required fields actually absent (a supplied `vendor` is no longer called missing), names any argument the tool does not take against the accepted set (`task` for `goal`), and points at reading the vendor's documentation — the SDK does not validate arguments against the inputSchema, so the handler's message is all the model gets. |
| [GRA-144](https://linear.app/get-modern/issue/GRA-144) | [#115](https://github.com/getmodern-ai/graft/pull/115) | — | the handoff link opens a focused page, not the console: the pending-action detail route moves from under the shell to under the guard alone at the same URL (`routes/_auth/pending.$id.tsx`), so a Hermes or OpenClaw link shows the one ask under the mark and closes itself once answered — the card popup still announcing to its opener first, a hand-opened tab showing *Done* — while `/pending` stays the console's inbox; `lib/handoff-page.ts` decides, tested (ADR 0006 as amended 2026-09-21). |
| [GRA-138](https://linear.app/get-modern/issue/GRA-138), [GRA-139](https://linear.app/get-modern/issue/GRA-139) | [#113](https://github.com/getmodern-ai/graft/pull/113) | — | the documentation reader sends `Accept-Language: en`, since developers.google.com redirected every page of 2026-09-20 to a locale picked for the server's address (nine pages, nine `?hl=`), and `htmlToText` drops `nav`, `header`, `footer` and `aside` whole and reads a `main` or lone `article` as the page — on `users.messages.list` the text went from 18,515 to 2,814 characters and the response body from past the 16,000 cut to character 1,790 |
| [GRA-141](https://linear.app/get-modern/issue/GRA-141), [GRA-140](https://linear.app/get-modern/issue/GRA-140) | [#114](https://github.com/getmodern-ai/graft/pull/114) | — | a `draft-missing` publish is the store's miss and the job absorbs it: it writes the draft it holds through the toolbox store (`McpDeps.toolbox` gains `writeTree`), publishes again, then waits once per `AcquireConfig.storeMissRetryDelaysMs` (250/500/1000/2000 ms by default) before the model is shown the refusal — with both GRA-123 halves deployed, job 27e15ac3 still lost two of three attempts to the miss; and the answer protocol and the skill ask for a proof read of every distinct path the module reads, since Seb's Gmail jobs proved the list and not the `get` whose headers they parsed |
| [GRA-147](https://linear.app/get-modern/issue/GRA-147) | [#120](https://github.com/getmodern-ai/graft/pull/120) | — | a link provider that cannot start its link steps aside in place: `mintProviderLink` rewrites the open ask onto the keyring (same row, same URL; `providerConnect: form`, a note, `providerFallback`) through `PendingActionDeps.updatePendingActionPayload`, the console's button answers `{ fallback: "form" }` and its card becomes the form, `start_link` points the chat card at Graft's own page, and a repeated `request_connection` is worded for the ask as it stands; the two link routes count `provider_link_started`, `provider_link_fell_back` and `provider_link_returned` (ADR 0019 bullet of 2026-09-21). A visible "Connect another way" is deferred, for the UX reasons the ticket records. |
| [GRA-132](https://linear.app/get-modern/issue/GRA-132) | [#106](https://github.com/getmodern-ai/graft/pull/106) | — | the agents list gains an Actions dropdown and an Edit modal for name, cap and idle window, with shared bounds and interaction tests |
| [GRA-133](https://linear.app/get-modern/issue/GRA-133) | [#108](https://github.com/getmodern-ai/graft/pull/108) | — | separate archive proposal; excluded from the GRA-135 branch by product direction, which now builds directly on GRA-132 |
| [GRA-135](https://linear.app/get-modern/issue/GRA-135) | [#109](https://github.com/getmodern-ai/graft/pull/109) | — | full-width agents table with harness badges, count/cap working sets and connection instructions from Not connected or the active agent's menu; the agent name and View agent action open the standalone management page for every row, with revoked records read-only; keeps table grouping, empty-state and copy refinements while excluding archiving; the drawer is deferred |
| [GRA-137](https://linear.app/get-modern/issue/GRA-137) | [#112](https://github.com/getmodern-ai/graft/pull/112) | — | the core is Apache-2.0 and a DCO replaces the CLA: the verbatim licence text with a two-line `NOTICE`, `CLA.md` and its bot deleted for a dependency-free `DCO` check over the pull request's own commits (a bot's passed over, a merge commit not), a `CONTRIBUTING.md` an outside contributor reads, and `"license": "Apache-2.0"` on every workspace manifest for scanners; ADR 0022 records the decision and supersedes ADR 0015, whose body stays and whose status and closing amendment point at it, and ADR 0011's internal commercial grant for Cando's dependency is withdrawn; the README is GRA-136's |
| [GRA-149](https://linear.app/get-modern/issue/GRA-149) | [#116](https://github.com/getmodern-ai/graft/pull/116) | — | rate limiting is a seam and the open form is unlimited by default: `@graft/ratelimit` is `RateLimiter.check({ bucket, key, now })` over six buckets, one per door, with `UNLIMITED` the default in both forms and `createMemoryRateLimiter` the in-process token bucket a self-hoster switches on one `GRAFT_RATE_LIMIT_<BUCKET>=<limit>/<windowSeconds>` at a time; `apps/server/src/rate-limit.ts` is one Hono middleware mounted above Better Auth's writes, `POST /mcp/oauth/{register,token}`, `/mcp`, `/api/proxy` and the API's session mutations, answering 429 with `Retry-After` in each door's own body shape and putting `rateLimited: { bucket, key }` on the wide event; keys are the person, the bearer's digest or the connection, and the client address otherwise, the socket's peer unless `GRAFT_TRUSTED_PROXY_HOPS` says how many hops are in front (ADR 0018's "a rate limit at the edge"; ADR 0002 as amended 2026-09-19) |
| [GRA-149](https://linear.app/get-modern/issue/GRA-149) | [#116](https://github.com/getmodern-ai/graft/pull/116) | — | rate limiting is a seam and the open form is unlimited by default: `@graft/ratelimit` is `RateLimiter.check({ bucket, key, now })` over six buckets, one per door, with `UNLIMITED` the default in both forms and `createMemoryRateLimiter` the in-process token bucket a self-hoster switches on one `GRAFT_RATE_LIMIT_<BUCKET>=<limit>/<windowSeconds>` at a time; `apps/server/src/rate-limit.ts` is one Hono middleware mounted above Better Auth's writes, `POST /mcp/oauth/{register,token}`, `/mcp`, `/api/proxy` and the API's session mutations, answering 429 with `Retry-After` in each door's own body shape and putting `rateLimited: { bucket, key }` on the wide event; keys are the person for `api` and the connection for `proxy`, and the client address for every door that runs before anyone is authenticated, `mcp` included and never a bearer's digest, since an unknown token still costs `requireAgent` a read and a key from the token would be a key the caller picks; the address is the socket's peer unless `GRAFT_TRUSTED_PROXY_HOPS` says how many hops are in front (ADR 0018's "a rate limit at the edge"; ADR 0002 as amended 2026-09-19) |
| [GRA-148](https://linear.app/get-modern/issue/GRA-148) | [#117](https://github.com/getmodern-ai/graft/pull/117) | — | safe defaults before the repository is public: the keys script mints `GRAFT_ADMIN_PASSWORD` so `cp .env.example .env && docker compose up -d` no longer opens an account whose password is committed, neither file carries a default for any secret, `@graft/env` refuses a short or retired one, and the database's host port moves to `docker-compose.dev.yml` on loopback; the session cookie is derived from the deployment's origins (`sessionCookieAttributes`: `lax` on one origin, `secure` from the scheme, and a console elsewhere over plain non-loopback http refused at boot); `apps/server/src/origin-guard.ts` refuses a state-changing `/api` request from any other origin, with the proxy, Better Auth and the health check exempt for reasons the file names, and `parseBody` answers 415 unless the body declares JSON; and a `SECURITY.md` saying where to report, what is in scope, and that no third-party audit has been done |
| [GRA-151](https://linear.app/get-modern/issue/GRA-151) | [#119](https://github.com/getmodern-ai/graft/pull/119) | — | the executor.sh teardown under `docs/research/` is withdrawn: a design-session note that read another project's code and pull-request history is not a document this repository publishes, and the ADRs already hold every decision it informed; ADR 0001, 0002 and 0006 keep their considered-options comparisons and lose the three sentences that made that project's history the argument, and the roadmap's determinism-gate row cites the design session instead of the file. |
| [GRA-153](https://linear.app/get-modern/issue/GRA-153) | [#121](https://github.com/getmodern-ai/graft/pull/121) | — | a proof-only answer: `prove { proofReads, note }` in the `proof` situation runs more reads against the draft as it stands and shows every read so far — a turn, not an attempt — so the path whose fields the module parses is proven with the id the first read returned instead of a second draft; `MAX_PROOF_READS` bounds an attempt's total and a `prove` past it is refused naming the room left; `proofReads` rides on the flat wire answer beside `urls` |
| [GRA-154](https://linear.app/get-modern/issue/GRA-154), [GRA-155](https://linear.app/get-modern/issue/GRA-155) | [#122](https://github.com/getmodern-ai/graft/pull/122) | — | `acquire` looks in the toolbox first: the vendor's live tools are ranked against the goal (`tools/acquire-similar.ts`, content words with a prefix-tolerant agreement, a read goal never answered with a write tool) and a close match is answered `similar_tools_exist` with its schema instead of built, `ignoreExisting: true` building anyway — an agent built a third copy of a Gmail listing tool on 2026-09-21; and the `mcp` wide event carries `detail` per tool (`find_tool`'s query and hits, `acquire`'s goal length, similar offer and job, `acquire_status`'s job and status, `run_tool`'s target) |
| [GRA-152](https://linear.app/get-modern/issue/GRA-152) | [#123](https://github.com/getmodern-ai/graft/pull/123) | — | the connection setup takes a harness — a `Select` over Hermes, OpenClaw and any MCP client, Hermes first — and the two blocks under the URL take its shape: for Hermes the `~/.hermes/.env` line and the YAML `mcp_servers` entry for `~/.hermes/config.yaml`, byte for byte as the docs site shows them; for the others the export line and the JSON block as before; stacked on #109, which owns `HarnessSetup` |
| [GRA-157](https://linear.app/get-modern/issue/GRA-157) | [#124](https://github.com/getmodern-ai/graft/pull/124) | — | a sign-up is a product event: `createAuth`'s `onPersonSignedUp` fires from Better Auth's own hooks when a person exists *and* is verified — `afterEmailVerification` for a password account, `databaseHooks.user.create.after` for a social account created verified — and the server captures `person_signed_up` with `method` and, alone among events, a person property through the seam's new `Capture.person`: the email, so the alpha's profiles have a name; `distinctId` stays the id, a rejecting hook never fails the request, and the hosted PostHog backing maps `person` onto `$set` (graft-cloud). The Slack message per sign-up is PostHog's own destination on the event, set up by hand. |
| [GRA-158](https://linear.app/get-modern/issue/GRA-158) | [#125](https://github.com/getmodern-ai/graft/pull/125) | — | a code of conduct for the public repository: the Contributor Covenant 2.1 verbatim with the enforcement contact set to the address SECURITY.md carries, linked from CONTRIBUTING.md in one sentence. |
| [GRA-159](https://linear.app/get-modern/issue/GRA-159) | [#126](https://github.com/getmodern-ai/graft/pull/126) | — | the `DCO` check passes over a merge commit GitHub itself makes (the "Update branch" button, committer `noreply@github.com`, two parents), since nobody can sign one; a merge a person makes locally is still theirs to sign, so the rule from #112 stands. Prompted by the first required run failing #116 on a local merge without `--signoff`, which the check was right about. |
| [GRA-168](https://linear.app/get-modern/issue/GRA-168) | [#130](https://github.com/getmodern-ai/graft/pull/130) | — | the harness setup is **Connect a harness** (dialog, menu item, header button) and a static-token agent's Harnesses cell is the prompt **Set up harness**, *Not recorded* once revoked — a *connection* is a vendor account, and "Not connected" was false for every working Hermes; the dev-only preview mock leaves the route and the orphaned `harness-snippet.tsx` is deleted; AGENTS.md names the copy and what an empty table draws |
| [GRA-162](https://linear.app/get-modern/issue/GRA-162), [GRA-164](https://linear.app/get-modern/issue/GRA-164) | [#128](https://github.com/getmodern-ai/graft/pull/128) | — | a fresh `initialize` announcing a protocol version the SDK lacks (Claude.ai's `2026-07-28`, on every session since 2026-09-20) has the header dropped so the body negotiates to Graft's latest instead of the transport's 400, a request that names a session keeping the SDK's check (ADR 0018 as amended 2026-09-22; the live check on Claude.ai is still to run); a `ServiceError` the API answers is a refusal on the wide event (`refusal: { code, message }`, `c.error` cleared) and no longer an error-level line with a stack, the transport refusal event carries `agentId` once the token resolved, and the console reads a handoff link once per visit rather than on every focus. |
| [GRA-173](https://linear.app/get-modern/issue/GRA-173) | [#133](https://github.com/getmodern-ai/graft/pull/133) | — | the public repository gains its issue forms (bug report, feature request, a `config.yml` pointing a vulnerability at `SECURITY.md` and a setup question at the README) and a `PULL_REQUEST_TEMPLATE.md` in this file's own build-record shape; AGENTS.md's Better Auth sentence read "1.7.3" as the installed version where the lockfile pins 1.7.5, reworded to "since 1.7.3" to keep the GRA-86 fact without the stale number |
