# GRA-45 report (Cando's primitives replace the console's; selects, confirmations and code blocks become primitives), PR #26

Saved by the orchestrator from the implementing agent's report, 2026-09-11.

**Report for the orchestrator — GRA-45**

**PR #26** — https://github.com/getmodern-ai/graft/pull/26 — head `829691fd7ead81365ec49611b3ddb9d0eaa052e6`, branch `aleks/gra-45-candos-primitives-replace-the-consoles-and-the-hand-rolled`, four commits (primitives port → hand-rolled controls → chips/call sites/AGENTS.md → Greptile fix). Reviewer `sebapoole` requested; `REVIEW_REQUIRED`, mergeable. **Not merged.** GRA-45 is **In Review** in Linear with the PR linked. Worktree `gra-45` left in place, clean; the console dev server I started on :3001 is stopped.

**CI / review.** `Typecheck, Lint & Test` green on both heads (4m29s, 4m36s), CLA pass. **Greptile 5/5 on `829691f`**, "no outstanding correctness or repository-rule issues". Its first pass (4/5) raised one P2: `KbdGroup` in the copied `kbd.tsx` typed as `div` props while rendering `<kbd>` — inherited verbatim from Cando (and the registry). Fixed as `ComponentProps<"kbd">` under a comment naming it the one code deviation, replied inline, PR body and AGENTS.md updated.

**Gate (final tree).** `check` no fixes left (the 2 pre-existing `packages/mcp` warnings only); `lint` clean; `check-types` 18/18; `test --force` 18/18, `Cached: 0` — web 74 (70 + 4 new `use-mobile`), server 114 incl. the Postgres integration suite on :5440 (it creates/drops its own throwaway DB); `check-colours` 96 sources, no findings; `check-tokens` 48/48 resolve; `db:check-chain` ok; conflict-marker and ADR checks pass (newest 0017). No lockfile change, no icon regeneration needed (every glyph the primitives import was already in the set).

**Primitives.** Replaced (16): alert, badge, button, card, checkbox, dialog, empty, field, input, label, separator, skeleton, spinner (byte-identical already), switch, table, textarea. Added (10): select, alert-dialog, tooltip, dropdown-menu, sheet, sidebar, kbd, popover, scroll-area, data-table (`DataTable`/`DataTableRow`/`DataTableSubHeader`). Plus `src/hooks/use-mobile.ts` + test. `button-sheen` `@utility` added to `index.css` verbatim; header no longer lists it as left out.

**Deviations from Cando's files.** Four import rewrites + Biome import order; comments naming Cando *app* files qualified as Cando's (dialog ×2, sidebar ×4, dropdown-menu, data-table, use-mobile); `cn-font-heading`/`cn-menu-*`/`cn-rtl-flip` kept verbatim (defined nowhere in Cando either — inert in both repos); `use-mobile.test.ts` scans `apps/web/src` only; `biome.json` mirrors Cando's `noDocumentCookie`/`useExhaustiveDependencies` override for `ui/**`; `button-sheen` placed after the file header; `KbdGroup` prop type (above). Choices: `AlertDialogMedia` unused, as in every Cando confirmation; Cancel label "Cancel" (was "Keep it"); page-level "Revoke token" stays solid destructive (Cando's org-delete pattern).

**Call sites.** Scheme and provider `<select>` → `Select` with `items`; two revoke dialogs → `AlertDialog` (question title, blast radius, Cancel first in DOM — verified `['Cancel','Revoke token']` — "Revoking…", both disabled, close ignored while pending); three `<pre>` → `components/code-block.tsx` (unknown-ask payload gains label + Copy); `success` chips for connected/allowed/ok/active; Add-connection dialog drops its own overflow classes; token-once wrapper takes `min-w-0` (a real defect the port exposed: the `export` line made the dialog grid scroll sideways). Behaviour verified live against :3100: token shown once, clipboard holds it, never on the agent page; selects drive the dependent fields; zero console errors/4xx. Two throwaway agents ("GRA-45 smoke …") were created and **revoked** in the local Docker DB; `hermes-laptop` and the connections untouched.

**Screenshots** (26) under graft-cloud `docs/screens/gra-45/`: `{login,signup,agents,create-agent-dialog,agent-detail-hermes-laptop,revoke-agent-alert-dialog,connections,add-connection-dialog,add-connection-scheme-select-open,pending,settings,settings-provider-select-open}-1440x1024-{light,dark}.png`, plus `create-agent-dialog-token-once-1440x1024-light.png` and `agent-detail-revoked-throwaway-1440x1024-light.png`. Delta list against GRA-44 is in the PR body.

**Left for GRA-46:** sidebar/sheet/tooltip/dropdown-menu/kbd/scroll-area/popover/use-mobile are in place and unused; shell, page header, auth doors untouched. **For GRA-47:** tables onto `DataTable`/`DataTableRow`, central chip map; noticed: `ConnectionCalls` now renders inside the banded `CardFooter`; model-key "set" and working-set count chips stay `secondary`; `ConnectionPicker`, `HostsNotice`, `RedirectUriNotice`, the relax box and the agents-table wrapper still draw their own `rounded-md`/`rounded-lg border` frames.

**For you to decide:** merge (approval from sebapoole pending); whether the `KbdGroup` fix should also be raised upstream in Cando; GRA-45's acceptance checkboxes in Linear are untouched.