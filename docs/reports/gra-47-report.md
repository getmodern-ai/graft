# GRA-47 report (screens follow Cando's patterns), PR #30 (stacked on the GRA-46 branch)

Saved by the orchestrator from the implementing agent's report, 2026-09-11.

## Report for the orchestrator — GRA-47

**PR #30** — https://github.com/getmodern-ai/graft/pull/30 — `feat(web): screens follow Cando's patterns: data tables, settings rows, empties, toasts and status chips (GRA-47)`. Base **`aleks/gra-46-the-console-wears-candos-shell-sidebar-mobile-top-bar`** (PR #28 was still `OPEN`, unmerged, at every check — no retarget done; retarget to `main` with `gh pr edit 30 --base main` and a hand merge of `origin/main` once #28 lands). Head **`e3df2fb8a2733527a7389ae2ac8f1eefa4d8f59b`**, two commits: `3ed2c46` the feature, `e3df2fb` the Greptile follow-up. Reviewer `sebapoole` requested; `OPEN`, `MERGEABLE`, no human review yet. **Not merged.** GRA-47 is **In Review** in Linear with #30 attached. Worktree `gra-47` left in place, clean; the main graft checkout and Cando's tree untouched.

**CI / Greptile.** `Typecheck, Lint & Test` pass on both heads (4m37s, 5m12s), CLA pass. Greptile first pass 3/5 with two P1s: (1) a toast Retry that recovered a loader-awaited read left the route boundary standing; (2) the agents route's now non-blocking connections read let the create dialog open and submit an empty scope. Both valid, both fixed in `e3df2fb`, replied inline with the SHA; **Greptile 5/5 on `e3df2fb`**, both threads resolved, no new comments. While verifying (1) I found sonner dismisses a toast whose action was pressed, which swallowed the replacement toast on a failed retry — `preventDefault` now keeps it, pinned in the test.

**Gate (final tree, worktree root).** `check` no fixes (the two pre-existing `packages/mcp` warnings); `lint` clean; `check-types` 18/18; `test --force` 18/18, **`Cached: 0`** — web 11 files / 98 tests (+ `status-chips.test.ts`, `query-error-retry.test.ts`), server 12 incl. the Postgres integration suite on the compose :5440; `check-colours` 122 sources clean; `check-tokens` 50/94 resolve; CI's marker and ADR checks pass.

**Decisions.** `ConnectionCalls` moved from the banded `CardFooter` (Cando's action strip; a table's row hover is the same `bg-muted/50` and vanishes on it) to a disclosure in the body. Double-toast rule: Cando's kept (toast + boundary), plus `onRecover` → `router.invalidate()` so a recovered toast Retry redraws the route. Agents and agent-detail loaders start their table reads without awaiting (Cando's CAN-546 shape) so skeleton rows are reachable; the create dialog draws skeleton scope and holds Create until connections arrive. Withdraw stays a visible button (menu of one is a click for nothing), `sr-only` header. Gaps: `gap-4` on list screens, `gap-6` on detail/settings. Chip labels sentence case (as Cando's `STATUS_COPY`).

**Deviations from Cando, and why.** Columns a row can't afford below `md` step out (`hidden md:table-cell`), the load-bearing one folding into the name cell — Cando's connections table narrows four columns, its history swaps to a list; five/six don't fit 358px either way (verified: no page overflow at 390, every table 310/310). `TableLoadingRows` is one full-span skeleton per row because of those hidden columns; `TableBodyNote` adds `whitespace-normal`. Settings keeps the page `h1` above the section `h2` (no settings sidebar/top bar here); field rows are `SettingsRow stacked` (description + error line need auto height) with `aria-labelledby`. Query-toast wording matches the mutation toast, not Cando's `Error:` prefix. `Promoted` is `secondary` not the filled `default`; model-key chip joins the map; `ConnectionPicker` uses `<ul>/<li>` Items rather than `ItemGroup`'s `div[role=list]`; `RemoveKeyButton` lost a duplicate `onError` toast; OpenAI label shortened to fit 240px.

**Verified live** (console :3001 → Docker :3100): 24/24 scripted checks plus 7/7 for the Greptile fixes — create agent + one-time token copy + revoke (every "GRA-47 smoke …" agent created was revoked; one leftover from a first run was revoked by the cleanup step); hermes-laptop's three tables with data; stubbed 503 → in-body Retry note + one keyed toast, Retry heals, failed retry replaces; calls table; settings provider change + cancel saves nothing; skeleton and empty states; loader-failure boundary cleared by the toast's Retry. `hermes-laptop` and the two real connections untouched; no approval relaxed/withdrawn, no ask created (the ticket's "add a connection / approve an ask / relax / revoke a connection" flows were not driven — they touch real data and no throwaway asks existed).

**Screenshots** (29) under graft-cloud `docs/screens/gra-47/`: `{agents,agent-detail-hermes-laptop,connections,pending,settings}-1440x1024-{light,dark}`, `agent-detail-hermes-laptop-1440xfull-{light,dark}`, `agents-loading-1440x1024-light`, `agents-empty-1440x1024-{light,dark}`, `agent-detail-working-set-error-toast-1440x1024-light`, `agent-detail-loader-error-boundary-toast-1440x1024-light`, `create-agent-dialog-{,scope-pending-}1440x1024-light`, `connections-calls-open-1440x1024-light`, `settings-form-1440x1024-light`, `settings-provider-select-open-1440x1024-light`, and 390×844 light: `agents`, `agent-detail-hermes-laptop`, `agent-detail-{working-set,approvals,history}`, `connections-calls-open`, `settings`. Delta list against `screens/gra-46/` is in the PR body.

**For you:** merge after sebapoole's approval and #28; GRA-47's acceptance checkboxes in Linear are untouched. Throwaway scripts (`walk.mjs`, `mobile.mjs`, `greptile.mjs`) were left in the orchestrator's temp directory if a re-capture is wanted.