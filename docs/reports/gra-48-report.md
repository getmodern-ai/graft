# GRA-48 report (the OAuth callback page is drawn by the console), PR #29

Saved by the orchestrator from the implementing agent's report, 2026-09-11.

## GRA-48 — report

**PR #29** — https://github.com/getmodern-ai/graft/pull/29 — `feat(web,server): the OAuth callback page is drawn by the console (GRA-48)`, base `main`, head **`c3137022c2d8b6f2fd54ff9eb93f364960555c69`** (two commits: `e617f4a` the feature, `c313702` the Greptile follow-up). Reviewer `sebapoole` requested; `OPEN`, `MERGEABLE`, `REVIEW_REQUIRED`. **Not merged.** GRA-48 is **In Review** in Linear with the PR attached. Worktree `gra-48` left in place (clean); the main graft checkout and Cando's tree untouched; `main` had not moved (PR #28 still open, no merge needed).

**CI and Greptile.** `Typecheck, Lint & Test` pass (4m54s on the final head), `CLA` pass, `Greptile Review` pass — **5/5**, "no new actionable issue remains". Its one P1 (a path-prefixed `GRAFT_CONSOLE_URL` would land on the not-found route) was answered with a reasoned reply, a comment/test-wording commit, and a new ticket; Greptile replied "That distinction is fair" and the thread is resolved.

**Gate (final tree, worktree root):** `check` formatting only (the two pre-existing `packages/mcp` warnings); `lint` clean; `check-types` 18/18; `test --force` 18/18, **`Cached: 0`** — server 12 files incl. the Postgres integration suite against a throwaway DB on the compose :5440, web 7, core 17; `check-colours` 97 sources clean; `check-tokens` 48/94 resolve; CI's conflict-marker and ADR checks pass; the server holds no hex colour.

**What changed.** Server callback → `302` to `${GRAFT_CONSOLE_URL}/oauth/callback?status=…&connectionId=…&message=…` for every outcome; `callbackPage()`/stylesheet/`escapeHtml` deleted; registered redirect URI unchanged. Contract centralised in `@graft/core`'s `oauth.rules.ts` (`OAuthCallbackMessage` moved there; `oauthCallbackRedirect` writer + `readOAuthCallbackSearch` reader colocated). New public route `apps/web/src/routes/oauth.callback.tsx` (outside `_auth`/shell): Cando `Empty` shape, `check_circle`/`warning`/`dangerous`, titles *Connected* / *Not connected* / *Something went wrong*, one-sentence description, "Close this window" (falls through to `/connections` if the browser refuses `close()`), `head` title per outcome. AGENTS.md OAuth paragraph and console-routes sentence updated; ADRs 0005/0006 don't describe the page's rendering, so untouched.

**Message contract kept:** `{ type: "graft:oauth", status: connected|declined|failed, connectionId, message }`, `BroadcastChannel("graft:oauth")`, `postMessage` target = the console origin (same value as before), auto-close 1500 ms on `connected` only. **One change on both sides, with a test:** `awaitConsent` now accepts the message from the console's own origin (where the page that posts it lives) instead of the server's; `serverOriginOf` retired and the hook no longer fetches the redirect URI first. `oauth-consent.test.ts` drives the route's reader over the server's writer and asserts the old server origin is rejected; `announceConsent` tested with fakes.

**Verified by hand** against the worktree's server (:3000, `GRAFT_CONSOLE_URL=http://localhost:3001`) and Vite, with a fake OAuth vendor (Node script behind an authenticated **ngrok** tunnel so the guarded fetch saw a public https host — no real Google client): connection-card path for all three outcomes (popup → vendor → server 302 → console route; connected auto-closed, toast, row `connected`; failed/declined showed the card's alert with the server's sentence, rows unchanged; vendor log shows PKCE authorize and the exchange with verifier + secret); add-connection dialog path (connected; dialog closed itself, toasts, row connected); the route directly from a same-origin opener (exactly one `message` + one channel event per outcome, correct payload, only `connected` closed, document title and `.light`/`.dark` correct). Zero browser console errors throughout. Throwaway persons/DBs dropped; dev servers, vendor and tunnel stopped.

**Not verified by hand:** the pending-action card path (same `useOAuthConsent` hook/`ConsentStatus`, not driven over MCP); a COOP-severing vendor (channel path exercised by the contract script, poll untouched); the compose image where the console is server-served (covered by the server suite's `CONSOLE_URL` assertions only). Stated in the PR.

**Screenshots** — graft-cloud `docs/screens/gra-48/`: `oauth-callback-{connected,declined,error}-1440x1024-{light,dark}.png`; `live-console-after-{connected,failed,declined}-1440x1024-light.png`; `live-dialog-filled-1440x1024-light.png`; `live-console-after-dialog-1440x1024-light.png`.

**Follow-on filed:** **GRA-50** (Graft, Backlog, related to GRA-48) — the router sets no `basepath`, so the path-prefixed `GRAFT_CONSOLE_URL` the env schema calls "legitimate" 404s every handoff URL and now the OAuth callback; two options and an acceptance test written in the ticket.